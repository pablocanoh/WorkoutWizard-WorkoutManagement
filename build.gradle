subprojects {
    apply plugin: 'java'

    group = 'edu.uoc'
    version = '0.0.1-SNAPSHOT'
    sourceCompatibility = '17'

    repositories {
        mavenCentral()
    }

    test {
        useJUnitPlatform()
    }
}

ext {
    springVersion = '3.1.6'
    lombok = '1.18.26'
}